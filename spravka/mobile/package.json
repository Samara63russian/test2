{
  "name": "spravka-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Android-оболочка для Сводных справок (Capacitor)",
  "scripts": {
    "sync": "npx cap sync android",
    "open": "npx cap open android",
    "build:web": "npm --prefix ../web run build && node scripts/copy-web.js",
    "add:android": "npx cap add android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.2",
    "@capacitor/core": "^7.4.2",
    "@capacitor/network": "^7.0.1",
    "@capacitor/preferences": "^7.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.2"
  }
}
